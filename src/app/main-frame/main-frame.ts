import {GridLocation} from "../grid-location/grid-location";
import {ConfigurableDevice} from "../configurable-device/configurable-device";

export class MainFrame extends ConfigurableDevice {
  gridLocation: GridLocation;
}
